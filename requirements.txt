# =========================
# Runtime dependencies
# =========================
# Face recognition / anti-spoof / feature extraction
insightface>=0.7.3
onnxruntime>=1.15.0
# If you need GPU provider for ONNX, replace the line above with:
# onnxruntime-gpu>=1.15.0
opencv-python>=4.8.0
numpy>=1.24.0

# Web API
fastapi>=0.104.0
pydantic>=2.0.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
qrcode>=7.4.2

# Excel export
openpyxl>=3.1.0

# =========================
# Utilities used by scripts
# =========================
Pillow>=10.0.0
scikit-learn>=1.3.0
httpx>=0.24.0

# =========================
# Optional acceleration
# =========================
# Install at least one when needed:
faiss-cpu>=1.8.0
annoy>=1.17.3
# torch>=2.0.0
# torchvision>=0.15.0

# Notes:
# 1) sqlite3 is built-in, no install needed.
# 2) torch/torchvision are optional and only needed for specific GPU/provider scenarios.
