# Upload This Folder

Use this `github/` directory as the repository root.
This package has removed local caches, model binaries, photos, and database files.

Important:
- `checkinexcel/.git`, `checkinexcel/.idea`, and `checkinexcel/target` may still exist locally due Windows permission locks.
- They are explicitly ignored in `.gitignore` and will not be committed when you run `git add .`.
